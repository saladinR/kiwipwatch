import { StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    text : {
      color :'black',
      marginTop :'30%',
      width : '100%',
      alignItems: 'center',
    },
    
});

export default styles;